#include<stdio.h> // gcc madhe asel header file tr angular bracket

#include "ppa.h" //  .c file madhe  .h include karaychi asel tr double quote madhe file name takaycha 
                // and this is called as user defined header file also
int main()
{
    struct Demo obj;

    printf("Inside main\n");
    
    printf("Rate of interest is : %f\n",ROI); //10.7

    printf("Fees of Logic BUilding batch is : %d\n",FEES); //18200
     
    printf("Size of demo structure is : %d\n",sizeof(obj)); //8
    
return 0;
}